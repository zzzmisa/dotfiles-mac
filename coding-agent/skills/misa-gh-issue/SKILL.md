---
name: misa-gh-issue
description: GitHub Issueの下書き・起票・更新と、調査に基づく改善Issueの提案に使う。リポジトリのテンプレートを優先し、Issueだけを求められた場合は実装しない。
---

# GitHub Issueの作成

## Workflow

Issueを書き始める前に:

1. 対象リポジトリを特定する。
2. 対象に適用されるリポジトリの指示を読む。既に読んだ内容は再利用する。
   `AGENTS.md` と `CLAUDE.md` に加えて、
   `CONTRIBUTING.md` や `.github/ISSUE_TEMPLATE/` があればコントリビューション
   ガイドとIssueテンプレートも読む。
3. 優先順位は、ユーザーからの明示的な指示 → 対象リポジトリのルールとテンプレート。
   リポジトリ固有の指定は、このスキルの既定フォーマットより優先する。

リポジトリに使えるIssueテンプレートがあれば、そのセクション構成と要求事項に従う。
無ければ [既定Issueテンプレート](references/ai-task-issue.md) を読み、Misaの既定テンプレートを使う。

リファクタリング・不要コード削除・パフォーマンス・アプリサイズの調査で、成果物として
Issueを求められている場合は、[改善Issueの調査](references/refactor-investigation.md) も読む。先に調査し、
Issueを書く（または起票する）ところで止める。実装はしない。

### Misaの既定テンプレート

リポジトリに使えるIssueテンプレートが無いときは、本文をこの順のセクションで構成する:

- `目的`
- `やること`
- `やらないこと`
- `完了条件`

`references/ai-task-issue.md` のフロントマターはテンプレートのメタデータであって、
Issue本文には含めない。Issueのタイトルは、ユーザーの実際のタスクから付ける。

コメントとプレースホルダは、具体的で簡潔な日本語で埋める。最終的なIssue本文から
HTMLコメントは削除する。`やらないこと` に書くことが無ければ `なし` と書く。

完了条件はタスクに合わせる。既定チェックリストは実装タスクの出発点とし、
文書だけの変更などでは不要なビルド・実機検証を要求しない。

実際に起票するよう頼まれていて、GitHubツールか `gh` が使えるなら、タイトルと本文を
用意したうえで起票する。下書きだけを頼まれている場合は、何も作らずMarkdownの本文を返す。

更新時は既存本文と対象番号を確認し、依頼範囲を反映する。新規起票前は関連する既存Issueを
確認し、重複を避ける。送信結果が不明なら、再送前に作成・更新済みかを調べる。
複数行の本文は構造化引数か一時ファイルに書き、`gh` では `--body-file` で渡す。
