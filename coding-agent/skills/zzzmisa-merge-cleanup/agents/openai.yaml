interface:
  display_name: "Misa: Merge Cleanup"
  short_description: "Clean up merged branches and stale worktrees"
  default_prompt: "Use $zzzmisa-merge-cleanup to remove only confirmed merged branches and their clean worktrees."
